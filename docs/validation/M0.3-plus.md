# M0.3-plus BACK 方案拦截验证记录

> 状态：待人工验收。以下表格为待填写模板，全部测试由人工在真机完成。

## 环境

| 项目 | 实测值 |
| --- | --- |
| 设备 | HONOR GT（AMG-AN00） |
| 系统 | Android 16 / API 36 |
| MagicOS | 10.0.0.170 |
| 豆包包名 | `com.larus.nova` |
| 豆包版本 | 待填写 |
| 默认拦截动作 | BACK（两下 `GLOBAL_ACTION_BACK`，间隔 400ms） |
| 备选拦截动作 | HOME（`GLOBAL_ACTION_HOME`，开关切换） |

## BACK 方案入口验证

测试方法：确认主页开关为「返回」后，从每个入口进入豆包，观察两下 BACK 后是否自然回到桌面。

| 入口 | 回到桌面 | 退出过程自然 | 观察记录（抖动/残留/卡页） | 结果 |
| --- | --- | --- | --- | --- |
| 桌面图标启动 | 待测 | 待测 | 待填写 | 待测 |
| 系统搜索 | 待测 | 待测 | 待填写 | 待测 |
| 最近任务 | 待测 | 待测 | 待填写 | 待测 |
| 外部 `doubao://` 链接（重点：深页面栈） | 待测 | 待测 | 待填写 | 待测 |
| 其他应用唤起/分享 | 待测 | 待测 | 待填写 | 待测 |
| 豆包通知 | 当前无可用通知样本 | 未实测 | 跳过 | 跳过 |

## 时序与体验观察

| 观察项 | 预期 | 实测 | 结果 |
| --- | --- | --- | --- |
| 两下 BACK 间隔 400ms 的节奏 | 第二下不被豆包忽略，整体节奏不拖沓 | 待填写 | 待测 |
| 豆包"再按一次退出"确认弹窗 | 不残留（若有，第二下 BACK 后应消失） | 待填写 | 待测 |
| 覆盖层提示 | 与 BACK 退出过程无冲突，3 秒自动消失 | 待填写 | 待测 |
| 拦截完成总时长 | 可接受（参考 <1.5 秒） | 待填写 | 待测 |

## 异常场景

| 场景 | 预期 | 实测 | 结果 |
| --- | --- | --- | --- |
| 从守卫主页「启动豆包」触发拦截（返回栈回到守卫） | 第二下 BACK 被跳过/取消，守卫不被退出（已修复首轮实测问题） | 待测 | 待测 |
| 豆包内停留在对话/二级页面时被拦截 | 两下 BACK 仍能退出（若不够能退出，记录实际按了几下/是否卡住） | 待测 | 待测 |
| 拦截后立即再次进入豆包 | 再次拦截，两下 BACK 再次生效 | 待测 | 待测 |
| BACK 方案期间不操作手机 | 第二下 BACK 按时执行，不受触摸干扰 | 待测 | 待测 |

## 开关回归验证

| 场景 | 预期 | 实测 | 结果 |
| --- | --- | --- | --- |
| 开关切回「回到桌面」（HOME） | 拦截立即回桌面，行为与 M0.3 一致 | 待测 | 待测 |
| 开关切回「返回」（BACK） | 恢复 BACK 方案 | 待测 | 待测 |
| 守卫应用重启后开关状态 | 回到默认 BACK（内存态，符合预期） | 待测 | 待测 |

## 非目标应用验证

| 场景 | 预期 | 实测 | 结果 |
| --- | --- | --- | --- |
| 正常使用设置、桌面、输入法等其他应用 | 不触发拦截、无误发 BACK | 待测 | 待测 |

## M0.3-plus 状态

**阶段性定稿**（2026-08-20 真机四轮实验后收口）：BACK 方案采用单次 `GLOBAL_ACTION_BACK`，
经四轮真机实验验证为唯一稳定退出且无误伤的方案（实验对比见 ROADMAP M0.3-plus 小节）；
计数 +2 为已接受边界。方案随 `84eee06` 提交定稿，拦截方式开关保留（默认 BACK，可切 HOME 对比）。

## 后续优化待办

- ~~补充时序日志，理清豆包退出全链路~~：已完成。2026-08-21 通过 IDE 明文日志还原完整时序，
  关键发现：①原"系统过渡窗口事件"实为守卫覆盖层自身的窗口事件（拦截后 54ms 显示并重置状态机）；
  ②第一发 BACK 被豆包用于内部页面回退（`MainActivity -> DrawerLayout`），class 变化是补发第二发的
  明确信号；③荣耀 `systemmanager` 跨应用启动豆包时弹两次 AlertDialog + 开屏广告页占前台约 1 秒
  （M0.4 授权启动验收需计入该干扰）。
- **精准补发 + 提示延迟显示（已实现，待真机验证）**：
  - 抑制期内目标 class 变化 -> 补发第二发 BACK（每会话上限 2 次）；
  - 覆盖层提示延迟到会话结束（豆包真正离开前台）后显示，每次退出只显示一次；
  - 待验证：热启动退出总时长（第二发由 class 变化约 +50ms 触发，快于原 +446ms 事件重置链）、
    提示显示时机（豆包退出后出现）、从守卫启动场景守卫安全、冷启动超时兜底链、HOME 开关回归。
