# learning-app-gate 项目路线图

## 项目概述

`learning-app-gate` 是一个面向个人侧载、公开源码的 Android 应用访问守门工具。首版专门服务 HONOR GT（AMG-AN00，Android 16 / MagicOS 10）上的豆包使用场景，通过访问时段、每日累计时长和学习任务控制孩子何时以及可以使用多久。

项目采用里程碑而非固定日期推进。每个里程碑必须通过对应验收后才能进入下一阶段，其中 M0 是决定后续开发是否继续的强制技术闸门。

## 技术基线

- 使用 Kotlin 和 Jetpack Compose 开发单模块 Android 应用。
- 使用 DataStore 持久化规则、每日使用记录和挑战状态。
- 首版不使用 Root、Device Owner 或荣耀企业 MDM。
- 由家长手动授权无障碍服务，检测目标应用进入前台并执行拦截。
- 首版界面只服务豆包，底层按目标应用包名设计，便于后续扩展。
- 所有规则、答题和使用时长数据只保存在本机，不采集豆包页面内容。

## M0：真机可行性原型

### 目标

验证 Android 16 和 MagicOS 10 是否允许自研应用完成“授权、启动、实时收回”的完整访问控制闭环：

```text
未授权直接启动 → 实时拦截
自研入口临时授权 → 启动豆包 → 允许使用
前台额度耗尽 → 实时收回权限 → 返回桌面
再次进入豆包 → 继续拦截
```

M0 必须证明守卫不仅能在启动时判断权限，还能在豆包已经处于前台时主动收回使用权。

### 工作内容

- 建立最小 Android 工程，确认豆包真实包名及系统启动 Intent。
- 在最小原型页面提供“授权并启动豆包”入口；点击后创建 `LaunchAuthorization`，再启动官方豆包。
- 实现无障碍服务并监听豆包前后台变化：
  - 无有效授权时，立即执行 `GLOBAL_ACTION_HOME` 并显示拦截提示。
  - 有有效授权且存在剩余时长时，允许使用并开始累计前台时长。
  - 豆包离开前台、锁屏或关屏时暂停累计。
  - 豆包恢复前台时从剩余时长继续累计。
  - 剩余时长归零时，先原子化撤销授权，再返回桌面并显示“本次使用时间已结束”。
- 授权在豆包不处于前台时因启动超时、守卫重置或其他规则失效的，立即清除；下一次进入按未授权访问拦截。
- 使用 `SystemClock.elapsedRealtime()` 计算前台时长，不使用可被修改的系统日期时间计算当前片段。
- 计时器触发时重新计算实际剩余时长，避免协程延迟、系统调度和息屏造成提前或延后收回。
- 验证从桌面、系统搜索、最近任务、通知和外部链接进入时均能被识别。
- 验证 MagicOS 重启、息屏、省电和后台清理后的服务状态。
- 豆包未安装、无可启动 Activity 或启动失败时，清除待生效授权并显示明确错误。

### 原型测试参数

- Debug 构建提供两种临时授权时长：
  - 30 秒，用于快速重复验证。
  - 10 分钟，用于模拟最终默认额度并完成正式真机验收。
- Release 构建不暴露 Debug 时长入口。
- M0 授权状态只保存在内存中；进程重启后恢复为未授权，持久化每日额度在 M2 实现。

### 验收标准

- 点击“授权并启动豆包”后，1 秒内进入官方豆包且不会被守卫误拦截。
- 未授权时，从桌面、搜索、最近任务、通知和外部链接进入豆包，均在 1 秒内被拦截。
- 豆包持续处于前台时，30 秒和 10 分钟额度耗尽后均在 1 秒内返回桌面。
- 收回权限后，从任何入口再次进入豆包都继续被拦截。
- 切到其他应用、锁屏或关屏期间不消耗额度，返回豆包后从剩余时长继续。
- 到期处理只执行一次，不产生无限返回、重复覆盖层、负数时长、闪退、短暂重新放行或其他应用误拦截。
- 手机重启、服务重连和原型进程被杀后默认回到未授权状态。
- 如果 MagicOS 后台清理导致无法在额度耗尽后 1 秒内实时拦截，M0 验收失败，暂停后续功能开发并重新评估无障碍、Device Owner 或荣耀 MDM 等技术路线。

## M1：访问时间限制

### 目标

仅允许孩子在每天指定时段内访问豆包。

### 工作内容

- 在 M0 原型访问控制器上增加时间规则。
- 建立通用 `TargetApp` 配置和规则判断内核，界面首版只配置豆包。
- 默认允许时间为每天 `07:20～20:30`，包含 07:20，不包含 20:30。
- 规定时间外启动豆包时返回桌面，显示当前不可使用及下一次开放时间。
- 豆包在 20:30 仍处于前台时立即拦截。
- 家长通过系统生物识别或设备凭据进入临时放行模式。

### 验收标准

- 07:19 无法访问，07:20 可以访问，20:30 立即禁止访问。
- 修改系统前后台状态不会绕过时间判断。
- 家长临时放行不改变孩子的默认规则。

## M2：每日累计时长

### 目标

把孩子每天使用豆包的前台累计时长限制为默认 10 分钟。

### 工作内容

- 将 M0 的临时额度改为 DataStore 持久化的每日累计额度，并保留实时收回机制。
- 仅在豆包位于前台时累计使用时间。
- 使用单调时钟计算当前前台片段，使用 DataStore 保存当天累计值，避免进程重启丢失。
- 切到后台、锁屏或关屏时暂停计时，重新进入后从剩余额度继续。
- 日期变化后按设备本地时区重置当日记录。
- 应用或手机重启后恢复正确的当日累计状态。
- 剩余 1 分钟时提醒；额度耗尽后返回桌面并显示“今日时间已用完”。

### 验收标准

- 多次进入豆包共享同一份每日额度。
- 后台、锁屏和关屏时间不计入额度。
- 进程终止和手机重启不会清零当天已用时长。
- 使用满 10 分钟后，当天无法通过普通入口继续访问。

## M3：家长设置入口

### 目标

让家长安全地调整规则并临时使用豆包，同时避免自建密码体系。

### 工作内容

- 使用 Android 系统生物识别或设备凭据验证家长身份。
- 支持修改允许时间段、每日额度和当日奖励时长。
- 支持家长本次不限时使用；退出豆包后结束本次家长放行。
- 配置修改立即生效；如果新规则已不允许当前使用，立即执行拦截。
- 设置页展示无障碍权限、目标应用安装状态和守卫运行状态。

### 验收标准

- 未通过系统身份验证不能查看或修改家长设置。
- 修改后的规则无需重启应用即可生效。
- 家长使用不消耗孩子的每日额度。
- 家长退出豆包后，后续访问重新执行孩子规则。

## M4：九九乘法门禁

### 目标

要求孩子每天第一次访问豆包前完成 5 道九九乘法题。

### 工作内容

- 每天第一次访问时随机生成 5 道 `1～9` 乘法题。
- 5 道全部答对后获得“今日已完成挑战”凭证。
- 当天再次进入无需答题，但仍受允许时间和每日累计额度限制。
- 答错时保留正确答案，标记错误并允许重新作答。
- 不通过固定题目顺序或固定答案位置泄露规律。
- 通过后复用 M0 已验证的授权启动链路打开官方豆包，不复制豆包内部界面。
- 完成每日挑战只获得当天准入资格，实际访问仍须同时满足允许时间和剩余额度。
- 日期变化、家长主动重置或应用数据清除后，挑战凭证失效。

### 验收标准

- 未完成当天挑战时，孩子无法通过其他豆包入口绕过答题。
- 5 道题全部正确后才生成当天凭证。
- 当天凭证不会绕过访问时间和每日额度。
- 次日首次进入时必须重新完成挑战。

## M5：可靠性与开源发布

### 目标

完成主力手机可长期使用的可靠性加固，并发布首个可侧载版本。

### 工作内容

- 完成首次启动引导，包括开启无障碍服务、允许受限制设置、配置荣耀自启动与电池优化。
- 增加守卫失效检测；无障碍被关闭时，在应用首页和通知中明确提示家长。
- 处理豆包未安装、包名变化、系统时间调整、服务被杀、应用升级和数据损坏。
- 补充隐私说明和侧载安装说明。
- 发布包含版本说明的 `v0.1.0` APK。
- 在首版稳定后，再评估多应用选择、其他品牌适配及荣耀 MDM 方案。

### 验收标准

- 普通家长可以根据引导独立完成安装和必要授权。
- 关键权限或守卫失效时不会静默失去保护。
- 连续日常使用期间不出现明显误拦截、计时丢失或异常耗电。
- 仓库包含构建、安装、权限、隐私和已知限制说明。

## 核心接口与状态

- `TargetApp`：目标应用包名、显示名称及启动能力。
- `LaunchAuthorization`：目标包名、授权总时长、累计前台时长、当前片段开始时间及授权状态；状态包括等待豆包在 5 秒内启动的 `Pending`、正在计时的 `Active`、暂停计时的 `Paused` 和不可复用的 `Revoked`。
- `AccessPolicy`：允许时间段、每日额度和题目数量。
- `AccessDecision`：允许、时间外、额度耗尽、需要答题或家长放行。
- `UsageLedger`：日期、累计前台时长和当前计时片段。
- `DailyChallengeState`：当天是否已完成挑战。
- `GateAccessibilityService`：只上报前后台事件并执行返回桌面，不承载授权转换、计时或业务规则。
- `AccessRuleEngine`：统一计算访问结果，供入口页面和无障碍服务共用。

## 测试策略

- 单元测试覆盖授权状态转换、授权过期与重复消费、前台实时到期、后台暂停、时间边界、跨午夜、每日重置、累计计时、规则修改和挑战状态。
- 仪器测试覆盖授权启动与启动失败、未授权启动、锁屏暂停、恢复计时、授权撤销竞态、进程重启、切后台、系统时间变化和 DataStore 恢复。
- HONOR GT 真机测试覆盖所有豆包入口、30 秒和 10 分钟实时收回、到期后重新进入、20:30 自动退出、家长验证及手机重启。
- 每个里程碑必须满足对应验收标准后才能进入下一阶段。

## 已知边界

- 普通无障碍方案只能阻止孩子继续交互并返回桌面，不能强行停止豆包进程。
- 主动关闭无障碍服务、卸载守卫应用或进入安全模式仍可能绕过保护。
- MagicOS 的后台和电池管理策略可能影响服务可靠性，必须通过 M0 真机验证并提供设置引导。
- 首版只以 HONOR GT（AMG-AN00，Android 16 / MagicOS 10）作为验收设备，不承诺其他设备或系统版本兼容性。

## 默认规则

- 每日允许时间：`07:20～20:30`。
- 每日累计额度：10 分钟。
- 每日乘法题数量：5 道。
- 乘法挑战每天只需完成一次。
- 家长操作使用系统生物识别或设备凭据验证。
- 未授权或超时时回到桌面，不连续模拟返回键。
- 首版豆包优先，但规则内核不写死豆包包名。
