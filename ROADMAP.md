# learning-app-gate 项目路线图

## 项目概述

`learning-app-gate` 是一个面向个人侧载、公开源码的 Android 应用访问守门工具。首版专门服务 HONOR GT（AMG-AN00，Android 16 / MagicOS 10）上的豆包使用场景，通过访问时段、每日累计时长和学习任务控制孩子何时以及可以使用多久。

项目采用里程碑而非固定日期推进。每个里程碑必须通过对应验收后才能进入下一阶段，其中 M0 是决定后续开发是否继续的强制技术闸门。

## 技术基线

- 使用 Kotlin 和 Jetpack Compose 开发单模块 Android 应用。
- 使用 DataStore 持久化规则、每日使用记录和挑战状态。
- 首版不使用 Root、Device Owner 或荣耀企业 MDM。
- 由家长手动授权无障碍服务，检测目标应用进入前台并执行拦截。
- 首版界面只服务豆包，底层按目标应用包名设计，便于后续扩展。
- 所有规则、答题和使用时长数据只保存在本机，不采集豆包页面内容。

## M0：真机可行性原型

**状态：已完成（2026-08-23）**。M0.1～M0.8 的功能闭环已实现，并在目标 HONOR GT 真机通过验收；汇总结论见 [docs/validation/M0.md](docs/validation/M0.md)。

### 目标

验证 Android 16 和 MagicOS 10 是否允许自研应用完成“授权、启动、实时收回”的完整访问控制闭环：

```text
未授权直接启动 → 实时拦截
自研入口临时授权 → 启动豆包 → 允许使用
前台额度耗尽 → 实时收回权限 → 逐层退出豆包
再次进入豆包 → 继续拦截
```

M0 必须证明守卫不仅能在启动时判断权限，还能在豆包已经处于前台时主动收回使用权。

M0 拆分为 8 个顺序执行的技术闸门。每一步只引入验证当前假设所需的最少代码，真机验收通过后才进入下一步。M0.3、M0.5 和 M0.8 是强制技术闸门，任一失败都不得通过增加业务功能掩盖。

### M0.1：目标应用识别

#### 工作内容

- 通过 ADB 确认豆包真实包名、主 Activity 和系统启动 Intent。
- 从桌面、系统搜索、最近任务、通知和外部链接进入豆包，记录实际前台包名和 Activity。
- 定义 `TargetApp`，集中保存目标包名、显示名称和启动能力，不在规则逻辑中散落豆包包名。

#### 验收标准

- 五种入口最终都进入同一目标包。
- 守卫能够通过系统 Intent 启动官方豆包。
- 此阶段不添加无障碍服务，也不执行拦截。

### M0.2：无障碍事件观测

#### 工作内容

- 注册最小 `GateAccessibilityService`，只监听窗口状态和窗口变化事件。
- 主页面显示无障碍服务启用状态，并提供跳转系统无障碍设置的入口。
- Debug 日志记录事件时间、包名、事件类型和当前屏幕状态，不读取或记录豆包页面内容。

#### 验收标准

- 五种入口进入豆包时，均能在 1 秒内收到目标包事件。
- 切换到其他应用不会误判为豆包。
- 如果 MagicOS 无法稳定上报目标包事件，M0 验收失败，不继续实现拦截。

### M0.3：未授权实时拦截

#### 工作内容

- 服务发现豆包进入前台且没有有效授权时，执行一次 `GLOBAL_ACTION_HOME`。
- 拦截后通过无障碍覆盖层显示友好提示；同一次前台会话只提示和拦截一次。
- 增加防重入状态，忽略守卫自身、桌面和拦截动作产生的后续窗口事件。

#### 验收标准

- 五种入口均在 1 秒内回到桌面。
- 不产生无限 HOME、重复提示、闪退或其他应用误拦截。
- 本阶段通过后，才能认为普通无障碍服务具备最低准入控制能力。

### M0.3-plus：BACK 方案拦截验证

#### 背景与目标

M0.3 采用 `GLOBAL_ACTION_HOME` 实现拦截并已验收通过，但直接回桌面在体验上比较突兀。真机观察发现豆包在前台时连按两下返回键即可自然退出，因此新增本阶段验证「`GLOBAL_ACTION_BACK`」作为替代拦截动作的可行性。

#### 工作内容

- 增加拦截动作开关（进程内存态，不持久化）：默认执行 BACK 方案，HOME 方案代码保留在另一分支可随时切回对比。
- BACK 方案为「单次 `GLOBAL_ACTION_BACK` + 抑制期内 class 变化精准补发」（依据 2026-08-21 时序日志优化）：
  - 时序日志证实第一发 BACK 常被豆包用于内部页面回退，class 变化的窗口事件是继续退出的明确信号，每会话上限 2 次精准补发；视频 → 会话 → 列表等三层页面最多执行 1 次主拦截加 2 次补发；
  - class 变化早于 400ms 安全间隔时保留原页面基线并延迟复检，避免真实导航信号被防抖逻辑提前消费；复检前台明确仍为豆包才允许补发；
  - 冷启动期间 BACK 无效且无 class 变化时，由 3 秒抑制超时兜底再次拦截；
  - 覆盖层提示延迟到会话结束（观测到豆包真正离开前台）后显示：时序日志证实拦截瞬间显示的提示窗口自身会产生 `TYPE_WINDOW_STATE_CHANGED`，被当作非目标前台提前重置状态机（原"系统过渡窗口事件"归因据此修正）；
  - 每次成功退出只显示一次提示；退出完成后提示窗口的事件落在 Idle 态，无干扰。
- 荣耀启动干扰（M0.4 验收考量）：跨应用启动豆包时 `com.hihonor.systemmanager` 会弹两次 AlertDialog 并插入开屏广告页（`AppSplashAdvertiseActivity`），广告页占住前台约 1 秒，"1 秒内进入豆包"的验收需计入该干扰。
- 本阶段验证由人工在真机完成。

#### 真机实验记录（方案对比）

| 方案 | 结果 | 结论 |
| --- | --- | --- |
| 固定两下 BACK（无校验） | 能退出但误伤 | 否决：第一下已退净时，第二下误退返回栈相邻应用（守卫/其他应用） |
| 单次 BACK + 离开去抖 | 退不出 | 否决：去抖吞掉豆包回前台事件，第二下无人提供 |
| 单次 BACK + 1 秒后 HOME 兜底 | 退不出且更乱 | 否决：BACK 与 HOME 交替行为更不可靠 |
| 两下 BACK（第二下抑制态校验）+ 去抖 | 退不出 | 否决：过渡事件提前重置抑制态导致第二下被取消，去抖又吞掉回前台事件 |
| 单次 BACK（无去抖） | 两下 BACK 才退出 | 曾采用（后由下行取代）：单发不退出豆包，第二下由过渡重置后的新拦截自然提供（计数 +2） |
| **单次 BACK + class 变化精准补发** | **按实际页面层级退出** | **采用**：单发被用于页面回退时，根据 class 变化继续补发（间隔 ≥400ms 防抖动连发），最多 1 次主拦截加 2 次补发；已验证视频 → 会话 → 列表三层路径完整退出且不穿透到其他应用 |

#### 验收标准

- 日常入口（热启动）进入豆包均能完全退出回到桌面，退出过程自然。
- 不误退守卫及其他应用；其他应用不误拦。
- 每次拦截会话按实际页面层级退出豆包（1 次主拦截 + 至多 2 次精准补发，拦截会话计数仅 +1）；开关切回 HOME 后原方案行为不变（回归）。

#### 结论处理

- 验证通过：M0.4 起拦截动作默认采用 BACK 方案，后续里程碑涉及拦截动作的描述届时统一更新。
- 验证不通过：保留 HOME 方案为默认，结论记录在验收文档。

### M0.4：临时授权并启动

#### 工作内容

- 引入内存态 `LaunchAuthorization`，状态包括 `Pending`、`Active`、`Paused` 和 `Revoked`。
- 主页面提供“授权并启动豆包”入口，先原子化创建 `Pending`，再发送官方启动 Intent。
- `Pending` 有效期为 5 秒；豆包及时进入前台则转为 `Active`，启动失败或超时则转为 `Revoked`。
- 只允许由守卫入口创建授权；直接启动豆包仍按 M0.3 拦截。

#### 验收标准

- 点击入口后 1 秒内进入官方豆包且不被守卫误拦截。
- 豆包未安装、无可启动 Activity 或启动失败时，撤销待生效授权并显示明确错误。

### M0.5：前台计时与实时收回

#### 工作内容

- Debug 页面提供 30 秒和 10 分钟两档临时授权；Release 构建不暴露 Debug 时长入口。
- 使用 `SystemClock.elapsedRealtime()` 记录授权总额、累计前台时长和当前片段起点。
- 独立的 `PrototypeAccessController` 负责授权转换与计时；无障碍服务只上报前台事件并执行拦截。
- 计时器触发时重新计算实际剩余时间；归零后先原子化转为 `Revoked`，再执行一次 BACK 拦截并提示“本次使用时间已结束”（2026-08-21 用户决策：到期动作用 BACK 替代原定 HOME——豆包由守卫入口启动，BACK 会自然退回守卫 App，且复用 M0.3-plus 单发 BACK + class 变化精准补发管线，多级页面也能退干净）。
- 授权且豆包处于前台时，通过不可触摸的无障碍覆盖层在顶部显示实时剩余时间；切后台、锁屏、到期、撤销授权或服务销毁时立即隐藏。

#### 验收标准

- 豆包持续处于前台时，30 秒和 10 分钟额度均在到期后 1 秒内被收回。
- 收回后从任何入口再次进入豆包都继续被拦截。
- 不出现负数时长、重复到期处理或短暂重新放行。

### M0.6：暂停与恢复计时

#### 工作内容

- 豆包离开前台时，将 `Active` 转为 `Paused` 并结算当前计时片段。
- 豆包重新进入前台时，从剩余时长继续并转回 `Active`。
- 锁屏或关屏时立即暂停；解锁本身不恢复计时，只有豆包重新成为前台才恢复。
- 授权在暂停期间失效或被重置时转为 `Revoked`，下次进入直接拦截。
- 2026-08-21 用户决策：`Paused` 无有效期（进程存活期间额度不冻结作废）；暂停期再次点击守卫入口 = 重置（旧额度作废，创建新授权）。

#### 验收标准

- 其他应用、锁屏和关屏期间不消耗额度。
- 多次快速切换前后台后，累计时间保持正确且每次状态转换唯一。

### M0.7：异常与竞态验证

#### 工作内容

- 覆盖启动超时、连续点击启动、到期瞬间切后台、到期与窗口事件同时发生、服务重连和目标应用崩溃。
- 控制器串行处理所有状态变更；`Revoked` 为终态，旧计时任务不得重新激活授权。
- 服务断开或原型进程被杀后不恢复内存授权，重新进入豆包时按未授权处理。
- 2026-08-21 实现决策：到期回调执行拦截前校验活跃窗口为豆包，非豆包（用户已切走）则跳过 BACK，杜绝到期时误退守卫/launcher（真机曾实测连发 3 次 BACK）；服务 `onDestroy` 即撤销全部内存授权；服务重连时若授权仍 Active 则按剩余额度补排到期任务（自愈）；豆包崩溃/被杀按暂停处理（额度冻结，结果确定且不惩罚用户）。

#### 验收标准

- 每个异常场景结果确定。
- 不产生重复覆盖层、残留计时任务、闪退或授权绕过。
- 2026-08-21 真机验收通过：到期瞬间切后台正确跳过 BACK（守卫不被退出）、服务断开撤销授权、进程被杀后按未授权拦截、无重复收回与残留计时任务。

### M0.8：MagicOS 可靠性闸门

#### 工作内容

- 真机验证手机重启、息屏、后台清理、省电模式、锁定后台任务和自启动设置。
- 分别测试持续前台到期、后台暂停后恢复，以及无障碍服务被系统回收后的重连。
- 每个场景至少重复 10 次，记录事件到达延迟、拦截延迟和失败次数。

#### 验收标准

- 正常配置下，所有未授权和到期拦截延迟均不超过 1 秒。
- 手机重启、服务重连或原型进程被杀后默认恢复为未授权。
- 如果 MagicOS 后台清理导致前台计时或实时收回不可靠，M0 验收失败，暂停 M1～M4，并重新评估前台服务、Device Owner 或荣耀 MDM。

### M0 验收记录

**最终结论：通过。** 未授权拦截、授权启动、前台计时、暂停恢复、到期收回、异常竞态和 MagicOS 真机可靠性闭环均已完成；M1～M4 可以继续推进。

- 每个阶段保留验收表，记录设备版本、豆包版本与包名、入口、预期结果、实测延迟、结果和日志编号。
- 控制器单元测试覆盖所有合法状态转换、重复事件、到期竞态和单调时钟计算。
- M0 全部状态只保存在内存中；进程重启后恢复为未授权，持久化每日额度仍在 M2 实现。
- M0 完成定义：未授权拦截、授权启动、前台到期实时收回、暂停恢复和系统恢复场景全部通过。

## M1：访问时间限制

### 目标

仅允许孩子在每天指定时段内访问豆包。

### 工作内容

- 在 M0 原型访问控制器上增加时间规则。
- 建立通用 `TargetApp` 配置和规则判断内核，界面首版只配置豆包。
- 默认允许时间为每天 `07:20～20:30`，包含 07:20，不包含 20:30。
- 规定时间外启动豆包时返回桌面，显示当前不可使用及下一次开放时间。
- 豆包在 20:30 仍处于前台时立即拦截。
- 家长通过系统生物识别或设备凭据进入临时放行模式。

### 验收标准

- 07:19 无法访问，07:20 可以访问，20:30 立即禁止访问。
- 修改系统前后台状态不会绕过时间判断。
- 家长临时放行不改变孩子的默认规则。

## M2：每日累计时长

### 目标

把孩子每天使用豆包的前台累计时长限制为默认 10 分钟。

### 工作内容

- 将 M0 的临时额度改为 DataStore 持久化的每日累计额度，并保留实时收回机制。
- 仅在豆包位于前台时累计使用时间。
- 使用单调时钟计算当前前台片段，使用 DataStore 保存当天累计值，避免进程重启丢失。
- 切到后台、锁屏或关屏时暂停计时，重新进入后从剩余额度继续。
- 日期变化后按设备本地时区重置当日记录。
- 应用或手机重启后恢复正确的当日累计状态。
- 剩余 1 分钟时提醒；额度耗尽后返回桌面并显示“今日时间已用完”。

### 验收标准

- 多次进入豆包共享同一份每日额度。
- 后台、锁屏和关屏时间不计入额度。
- 进程终止和手机重启不会清零当天已用时长。
- 使用满 10 分钟后，当天无法通过普通入口继续访问。

## M3：家长设置入口

### 目标

让家长安全地调整规则并临时使用豆包，同时避免自建密码体系。

### 工作内容

- 使用 Android 系统生物识别或设备凭据验证家长身份。
- 支持修改允许时间段、每日额度和当日奖励时长。
- 支持家长本次不限时使用；退出豆包后结束本次家长放行。
- 配置修改立即生效；如果新规则已不允许当前使用，立即执行拦截。
- 设置页展示无障碍权限、目标应用安装状态和守卫运行状态。

### 验收标准

- 未通过系统身份验证不能查看或修改家长设置。
- 修改后的规则无需重启应用即可生效。
- 家长使用不消耗孩子的每日额度。
- 家长退出豆包后，后续访问重新执行孩子规则。

## M4：九九乘法门禁

### 目标

要求孩子每天第一次访问豆包前完成 5 道九九乘法题。

### 工作内容

- 每天第一次访问时随机生成 5 道 `1～9` 乘法题。
- 5 道全部答对后获得“今日已完成挑战”凭证。
- 当天再次进入无需答题，但仍受允许时间和每日累计额度限制。
- 答错时保留正确答案，标记错误并允许重新作答。
- 不通过固定题目顺序或固定答案位置泄露规律。
- 通过后复用 M0 已验证的授权启动链路打开官方豆包，不复制豆包内部界面。
- 完成每日挑战只获得当天准入资格，实际访问仍须同时满足允许时间和剩余额度。
- 日期变化、家长主动重置或应用数据清除后，挑战凭证失效。

### 验收标准

- 未完成当天挑战时，孩子无法通过其他豆包入口绕过答题。
- 5 道题全部正确后才生成当天凭证。
- 当天凭证不会绕过访问时间和每日额度。
- 次日首次进入时必须重新完成挑战。

## M5：可靠性与开源发布

### 目标

完成主力手机可长期使用的可靠性加固，并发布首个可侧载版本。

### 工作内容

- 完成首次启动引导，包括开启无障碍服务、允许受限制设置、配置荣耀自启动与电池优化。
- 增加守卫失效检测；无障碍被关闭时，在应用首页和通知中明确提示家长。
- 处理豆包未安装、包名变化、系统时间调整、服务被杀、应用升级和数据损坏。
- 补充隐私说明和侧载安装说明。
- 发布包含版本说明的 `v0.1.0` APK。
- 在首版稳定后，再评估多应用选择、其他品牌适配及荣耀 MDM 方案。

### 验收标准

- 普通家长可以根据引导独立完成安装和必要授权。
- 关键权限或守卫失效时不会静默失去保护。
- 连续日常使用期间不出现明显误拦截、计时丢失或异常耗电。
- 仓库包含构建、安装、权限、隐私和已知限制说明。

## 核心接口与状态

- `TargetApp`：目标应用包名、显示名称及启动 Intent 解析能力。
- `LaunchAuthorization`：目标包名、授权总时长、累计前台时长、当前片段开始时间及授权状态；状态包括等待豆包在 5 秒内启动的 `Pending`、正在计时的 `Active`、暂停计时的 `Paused` 和不可复用的 `Revoked`。
- `PrototypeAccessController`：串行处理 M0 的授权、前后台、屏幕状态、超时和撤销事件。
- `GateStatus`：向 Compose 页面暴露无障碍服务状态、授权状态、剩余时长和最近一次错误。
- `AccessPolicy`：允许时间段、每日额度和题目数量。
- `AccessDecision`：允许、时间外、额度耗尽、需要答题或家长放行。
- `UsageLedger`：日期、累计前台时长和当前计时片段。
- `DailyChallengeState`：当天是否已完成挑战。
- `GateAccessibilityService`：只观察目标应用、上报前后台事件并执行返回桌面或覆盖层提示，不承载授权转换、计时或业务规则。
- `AccessRuleEngine`：统一计算访问结果，供入口页面和无障碍服务共用。

## 测试策略

- 单元测试覆盖授权状态转换、授权过期与重复消费、前台实时到期、后台暂停、时间边界、跨午夜、每日重置、累计计时、规则修改和挑战状态。
- 仪器测试覆盖授权启动与启动失败、未授权启动、锁屏暂停、恢复计时、授权撤销竞态、进程重启、切后台、系统时间变化和 DataStore 恢复。
- HONOR GT 真机测试覆盖所有豆包入口、30 秒和 10 分钟实时收回、到期后重新进入、20:30 自动退出、家长验证及手机重启。
- 每个里程碑必须满足对应验收标准后才能进入下一阶段。

## 已知边界

- 普通无障碍方案只能阻止孩子继续交互并返回桌面，不能强行停止豆包进程。
- 主动关闭无障碍服务、卸载守卫应用或进入安全模式仍可能绕过保护。
- MagicOS 的后台和电池管理策略可能影响服务可靠性，必须通过 M0 真机验证并提供设置引导。
- 首版只以 HONOR GT（AMG-AN00，Android 16 / MagicOS 10）作为验收设备，不承诺其他设备或系统版本兼容性。

## 默认规则

- 每日允许时间：`07:20～20:30`。
- 每日累计额度：10 分钟。
- 每日乘法题数量：5 道。
- 乘法挑战每天只需完成一次。
- 家长操作使用系统生物识别或设备凭据验证。
- 未授权或超时时回到桌面，不连续模拟返回键。
- 首版豆包优先，但规则内核不写死豆包包名。
